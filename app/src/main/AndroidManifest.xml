<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginregistersos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LOGINREGISTERSOS">
        <activity android:name=".EditEmergencyMessage"></activity>
        <activity android:name=".ViewEmergencyContact" />
        <activity android:name=".EmergencyMessage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EmergencyTutorialVideo" />
        <activity android:name=".Login1" />
        <activity android:name=".Register1" />
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>